import logo from "../../logo.svg";

export function LogInPage() {
  return (
    <div className="App">
      <header className="App-header">
        <img src={logo} className="App-logo" alt="logo" />
        <p>{` Log In / Register Page`}</p>
      </header>
    </div>
  );
}
